{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "MCP servers for Midas project",
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp"],
      "description": "Browser automation via Playwright"
    }
  }
}
